# Markdown Standards

Use one top-level heading per file. Use descriptive headings in logical order. Do not skip heading levels. Use lists only when they improve clarity.

Every source article must include YAML front matter.
