# Repository Standards

The repository uses a single-source model. Reusable documentation articles belong in `content/`. Manuals and other outputs are defined in `collections/`.

Do not duplicate source content unless a separate audience-specific article is required.
