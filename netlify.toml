[build]
  command = "python -m pip install -r requirements.txt && mkdocs build --clean --strict --site-dir dist/board-members"
  publish = "dist"

[build.environment]
  PYTHON_VERSION = "3.12"

[[redirects]]
  from = "/"
  to = "/board-members/"
  status = 302
  force = true
